# Fieldnote

An ephemeral, no-build VanJS editor for durable plain-text notes.

The app is intended to be hosted as static files. Credentials should be entered
per session and kept in browser memory only.
